function yhat = clasificacionBayesiana( modelo, X)
% Con los modelos entrenados, predice la clase para cada muestra X
    ypred = [];
    for i = length(modelo)
        ypred(:, i) gaussLog(modelo{i}.mu,modelo{i}.Sigma,X))
    end
    [~,yhat] = max(ypred,[],2);
end




